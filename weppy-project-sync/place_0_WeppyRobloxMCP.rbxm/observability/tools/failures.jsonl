{"id":"464b36f1-2b4b-462e-8573-8dcba7d7fdd4","sessionId":"B1B1-42FB","placeId":0,"placeName":"WeppyRobloxMCP.rbxm","timestamp":"2026-05-07T06:38:59.243Z","tool":"manage_scripts","command":"manage_scripts_create","tier":"basic","parameters":{"path":"game.ServerScriptService.SOVEREIGN_MANIFEST.Primer_R6EnemyChassis","script_type":"Script","source":"-- =============================================================================\n-- Primer_R6EnemyChassis\n-- Rebuilds the R6 enemy rig primer from free Marketplace assets.\n-- Pulls a humanoid R6 rig, configures it as a combat-ready enemy chassis,\n-- and registers it in ServerStorage.MonsterTemplates for VictimService.\n--\n-- AERIAL_HAWK_PRESET note:\n--   Set ARCHETYPE = \"aerial\" to apply wing physics offsets and disable\n--   Humanoid.WalkSpeed (aerial enemies use BodyVelocity instead).\n-- =============================================================================\n\nlocal ENABLE_ON_BOOT = false   -- flip to true when you want it to run\nif not ENABLE_ON_BOOT then return end\n\n-- ── Config ────────────────────────────────────────────────────────────────────\n\nlocal ARCHETYPES = {\n\tground = {\n\t\tname            = \"Primer_Ground_R6\",\n\t\tmaxHealth       = 200,\n\t\twalkSpeed       = 16,\n\t\tuseBodyVelocity = false,\n\t\t-- Free R6 dummy rig (Roblox default mannequin — always available)\n\t\t-- Replace with a specific Marketplace asset ID once sourced.\n\t\tmarketplaceAssetId = 0,   -- 0 = build blocky fallback inline\n\t},\n\taerial = {\n\t\tname            = \"Primer_Aerial_R6\",\n\t\tmaxHealth       = 120,\n\t\twalkSpeed       = 0,      -- aerial: WalkSpeed locked, BodyVelocity drives movement\n\t\tuseBodyVelocity = true,\n\t\tmarketplaceAssetId = 0,   -- 0 = build blocky fallback inline\n\t\thoverHeight     = 18,     -- studs above terrain\n\t\tdiveSpeed       = 60,     -- studs/s during dive attack\n\t},\n}\n\nlocal ARCHETYPE = \"aerial\"   -- change to \"ground\" for ground chassis\n\n-- ── Services ──────────────────────────────────────────────────────────────────\n\nlocal InsertService  = game:GetService(\"InsertService\")\nlocal ServerStorage  = game:GetService(\"ServerStorage\")\n\n-- ── Helpers ───────────────────────────────────────────────────────────────────\n\nlocal function ensureFolder(parent, name)\n\tlocal f = parent:FindFirstChild(name)\n\tif f and f:IsA(\"Folder\") then return f end\n\tf = Instance.new(\"Folder\")\n\tf.Name = name\n\tf.Parent = parent\n\treturn f\nend\n\nlocal function buildBlockyR6Fallback(cfg)\n\t-- Minimal R6-compatible rig when no Marketplace asset is available.\n\t-- Matches standard R6 part names so existing animation/VictimService code works.\n\tlocal model = Instance.new(\"Model\")\n\tmodel.Name  = cfg.name\n\n\tlocal function part(name, size, cframe, color)\n\t\tlocal p = Instance.new(\"Part\")\n\t\tp.Name      = name\n\t\tp.Size      = size\n\t\tp.CFrame    = cframe\n\t\tp.BrickColor = BrickColor.new(color or \"Medium stone grey\")\n\t\tp.Anchored  = false\n\t\tp.CanCollide = true\n\t\tp.Parent    = model\n\t\treturn p\n\tend\n\n\tlocal function weld(p0, p1)\n\t\tlocal w = Instance.new(\"WeldConstraint\")\n\t\tw.Part0  = p0\n\t\tw.Part1  = p1\n\t\tw.Parent = p0\n\tend\n\n\tlocal root  = part(\"HumanoidRootPart\", Vector3.new(2,2,1),   CFrame.new(0,5,0), \"Bright blue\")\n\tlocal torso = part(\"Torso\",            Vector3.new(2,2,1),   CFrame.new(0,5,0))\n\tlocal head  = part(\"Head\",             Vector3.new(2,1,1),   CFrame.new(0,6.5,0))\n\tlocal lArm  = part(\"Left Arm\",         Vector3.new(1,2,1),   CFrame.new(-1.5,5,0))\n\tlocal rArm  = part(\"Right Arm\",        Vector3.new(1,2,1),   CFrame.new(1.5,5,0))\n\tlocal lLeg  = part(\"Left Leg\",         Vector3.new(1,2,1),   CFrame.new(-0.5,3,0))\n\tlocal rLeg  = part(\"Right Leg\",        Vector3.new(1,2,1),   CFrame.new(0.5,3,0))\n\n\tweld(root, torso); weld(torso, head)\n\tweld(torso, lArm); weld(torso, rArm)\n\tweld(torso, lLeg); weld(torso, rLeg)\n\n\t-- Aerial chassis: add wing stand-ins (replaced by real mesh later)\n\tif cfg.useBodyVelocity then\n\t\tlocal lWing = part(\"Wing_L\", Vector3.new(4,0.3,2), CFrame.new(-3,5,0), \"Bright yellow\")\n\t\tlocal rWing = part(\"Wing_R\", Vector3.new(4,0.3,2), CFrame.new(3,5,0),  \"Bright yellow\")\n\t\tlWing.Transparency = 0.4\n\t\trWing.Transparency = 0.4\n\t\tweld(torso, lWing); weld(torso, rWing)\n\n\t\t-- BodyVelocity for aerial movement (Humanoid.WalkSpeed is NOT used)\n\t\tlocal bv = Instance.new(\"BodyVelocity\")\n\t\tbv.Name         = \"AerialDrive\"\n\t\tbv.Velocity     = Vector3.new(0,0,0)\n\t\tbv.MaxForce     = Vector3.new(1e4,1e4,1e4)\n\t\tbv.Parent       = root\n\n\t\t-- BodyGyro to hold orientation\n\t\tlocal bg = Instance.new(\"BodyGyro\")\n\t\tbg.Name      = \"AerialGyro\"\n\t\tbg.MaxTorque = Vector3.new(1e5,1e5,1e5)\n\t\tbg.D         = 100\n\t\tbg.Parent    = root\n\tend\n\n\t-- Humanoid\n\tlocal hum = Instance.new(\"Humanoid\")\n\thum.RigType   = Enum.HumanoidRigType.R6\n\thum.MaxHealth = cfg.maxHealth\n\thum.Health    = cfg.maxHealth\n\thum.WalkSpeed = cfg.walkSpeed\n\thum.Parent    = model\n\n\tmodel.PrimaryPart = root\n\treturn model\nend\n\nlocal function buildFromMarketplace(assetId, cfg)\n\tlocal ok, result = pcall(function()\n\t\treturn InsertService:LoadAsset(assetId)\n\tend)\n\tif not ok then\n\t\twarn(string.format(\"[PRIMER] Marketplace load failed (assetId=%d): %s — using blocky fallback\", assetId, tostring(result)))\n\t\treturn nil\n\tend\n\t-- LoadAsset returns a Model container\n\tlocal container = result\n\tlocal rig = container:FindFirstChildOfClass(\"Model\")\n\tif rig then\n\t\trig.Parent = nil\n\tend\n\tcontainer:Destroy()\n\n\tif not rig then\n\t\twarn(\"[PRIMER] No Model found inside Marketplace asset — using blocky fallback\")\n\t\treturn nil\n\tend\n\n\t-- Ensure Humanoid is configured\n\tlocal hum = rig:FindFirstChildOfClass(\"Humanoid\")\n\t\tor Instance.new(\"Humanoid\", rig)\n\thum.MaxHealth = cfg.maxHealth\n\thum.Health    = cfg.maxHealth\n\thum.WalkSpeed = cfg.walkSpeed\n\thum.RigType   = Enum.HumanoidRigType.R6\n\n\t-- Ensure PrimaryPart\n\tlocal hrp = rig:FindFirstChild(\"HumanoidRootPart\")\n\tif hrp then rig.PrimaryPart = hrp end\n\n\t-- Aerial: inject BodyVelocity if not present\n\tif cfg.useBodyVelocity and hrp then\n\t\tif not hrp:FindFirstChild(\"AerialDrive\") then\n\t\t\tlocal bv = Instance.new(\"BodyVelocity\")\n\t\t\tbv.Name     = \"AerialDrive\"\n\t\t\tbv.Velocity = Vector3.new(0,0,0)\n\t\t\tbv.MaxForce = Vector3.new(1e4,1e4,1e4)\n\t\t\tbv.Parent   = hrp\n\t\tend\n\t\tif not hrp:FindFirstChild(\"AerialGyro\") then\n\t\t\tlocal bg = Instance.new(\"BodyGyro\")\n\t\t\tbg.Name      = \"AerialGyro\"\n\t\t\tbg.MaxTorque = Vector3.new(1e5,1e5,1e5)\n\t\t\tbg.D         = 100\n\t\t\tbg.Parent    = hrp\n\t\tend\n\tend\n\n\trig.Name = cfg.name\n\treturn rig\nend\n\n-- ── Main ──────────────────────────────────────────────────────────────────────\n\ntask.wait(1)\n\nlocal cfg = ARCHETYPES[ARCHETYPE]\nassert(cfg, \"[PRIMER] Unknown ARCHETYPE: \" .. tostring(ARCHETYPE))\n\nprint(string.format(\"[PRIMER] Building %s chassis (assetId=%d)…\", cfg.name, cfg.marketplaceAssetId))\n\nlocal rig\nif cfg.marketplaceAssetId and cfg.marketplaceAssetId > 0 then\n\trig = buildFromMarketplace(cfg.marketplaceAssetId, cfg)\nend\nrig = rig or buildBlockyR6Fallback(cfg)\n\n-- Register in MonsterTemplates\nlocal templates = ensureFolder(ServerStorage, \"MonsterTemplates\")\nlocal existing  = templates:FindFirstChild(cfg.name)\nif existing then existing:Destroy() end\nrig.Parent = templates\n\n-- Stamp archetype metadata as attributes (readable by VictimService/director)\nrig:SetAttribute(\"OMC_Archetype\",       ARCHETYPE)\nrig:SetAttribute(\"OMC_MaxHealth\",       cfg.maxHealth)\nrig:SetAttribute(\"OMC_UseBodyVelocity\", cfg.useBodyVelocity or false)\nif cfg.useBodyVelocity then\n\trig:SetAttribute(\"OMC_HoverHeight\", cfg.hoverHeight or 18)\n\trig:SetAttribute(\"OMC_DiveSpeed\",   cfg.diveSpeed   or 60)\nend\nrig:SetAttribute(\"OMC_MarketplaceAssetId\", cfg.marketplaceAssetId or 0)\nrig:SetAttribute(\"OMC_PrimerVersion\",  \"1.0.0\")\n\nprint(string.format(\"[PRIMER] ✓ %s ready in ServerStorage.MonsterTemplates\", cfg.name))\nprint(string.format(\"[PRIMER]   Attributes: Archetype=%s | Health=%d | BodyVelocity=%s\",\n\tARCHETYPE, cfg.maxHealth, tostring(cfg.useBodyVelocity)))\n"},"errorMessage":"scriptType and parent are required","errorCode":"EXECUTION_FAILED","executionTimeMs":0}
{"id":"319b5727-9209-4451-ba0e-533ed7474985","sessionId":"B1B1-42FB","placeId":0,"placeName":"WeppyRobloxMCP.rbxm","timestamp":"2026-05-07T06:39:30.602Z","tool":"manage_scripts","command":"manage_scripts_create","tier":"basic","parameters":{"path":"game.ServerScriptService.SOVEREIGN_MANIFEST.Primer_R6EnemyChassis","script_type":"Script","parent":"game.ServerScriptService.SOVEREIGN_MANIFEST","source":"-- =============================================================================\n-- Primer_R6EnemyChassis\n-- Rebuilds the R6 enemy rig primer from free Marketplace assets.\n-- Pulls a humanoid R6 rig, configures it as a combat-ready enemy chassis,\n-- and registers it in ServerStorage.MonsterTemplates for VictimService.\n--\n-- AERIAL_HAWK_PRESET note:\n--   Set ARCHETYPE = \"aerial\" to apply wing physics offsets and disable\n--   Humanoid.WalkSpeed (aerial enemies use BodyVelocity instead).\n-- =============================================================================\n\nlocal ENABLE_ON_BOOT = false   -- flip to true when you want it to run\nif not ENABLE_ON_BOOT then return end\n\n-- ── Config ────────────────────────────────────────────────────────────────────\n\nlocal ARCHETYPES = {\n\tground = {\n\t\tname            = \"Primer_Ground_R6\",\n\t\tmaxHealth       = 200,\n\t\twalkSpeed       = 16,\n\t\tuseBodyVelocity = false,\n\t\tmarketplaceAssetId = 0,\n\t},\n\taerial = {\n\t\tname            = \"Primer_Aerial_R6\",\n\t\tmaxHealth       = 120,\n\t\twalkSpeed       = 0,\n\t\tuseBodyVelocity = true,\n\t\tmarketplaceAssetId = 0,\n\t\thoverHeight     = 18,\n\t\tdiveSpeed       = 60,\n\t},\n}\n\nlocal ARCHETYPE = \"aerial\"\n\n-- ── Services ──────────────────────────────────────────────────────────────────\n\nlocal InsertService  = game:GetService(\"InsertService\")\nlocal ServerStorage  = game:GetService(\"ServerStorage\")\n\n-- ── Helpers ───────────────────────────────────────────────────────────────────\n\nlocal function ensureFolder(parent, name)\n\tlocal f = parent:FindFirstChild(name)\n\tif f and f:IsA(\"Folder\") then return f end\n\tf = Instance.new(\"Folder\")\n\tf.Name = name\n\tf.Parent = parent\n\treturn f\nend\n\nlocal function buildBlockyR6Fallback(cfg)\n\tlocal model = Instance.new(\"Model\")\n\tmodel.Name  = cfg.name\n\n\tlocal function part(name, size, cframe, color)\n\t\tlocal p = Instance.new(\"Part\")\n\t\tp.Name       = name\n\t\tp.Size       = size\n\t\tp.CFrame     = cframe\n\t\tp.BrickColor = BrickColor.new(color or \"Medium stone grey\")\n\t\tp.Anchored   = false\n\t\tp.CanCollide = true\n\t\tp.Parent     = model\n\t\treturn p\n\tend\n\n\tlocal function weld(p0, p1)\n\t\tlocal w = Instance.new(\"WeldConstraint\")\n\t\tw.Part0  = p0\n\t\tw.Part1  = p1\n\t\tw.Parent = p0\n\tend\n\n\tlocal root  = part(\"HumanoidRootPart\", Vector3.new(2,2,1), CFrame.new(0,5,0), \"Bright blue\")\n\tlocal torso = part(\"Torso\",            Vector3.new(2,2,1), CFrame.new(0,5,0))\n\tlocal head  = part(\"Head\",             Vector3.new(2,1,1), CFrame.new(0,6.5,0))\n\tlocal lArm  = part(\"Left Arm\",         Vector3.new(1,2,1), CFrame.new(-1.5,5,0))\n\tlocal rArm  = part(\"Right Arm\",        Vector3.new(1,2,1), CFrame.new(1.5,5,0))\n\tlocal lLeg  = part(\"Left Leg\",         Vector3.new(1,2,1), CFrame.new(-0.5,3,0))\n\tlocal rLeg  = part(\"Right Leg\",        Vector3.new(1,2,1), CFrame.new(0.5,3,0))\n\n\tweld(root, torso); weld(torso, head)\n\tweld(torso, lArm); weld(torso, rArm)\n\tweld(torso, lLeg); weld(torso, rLeg)\n\n\tif cfg.useBodyVelocity then\n\t\tlocal lWing = part(\"Wing_L\", Vector3.new(4,0.3,2), CFrame.new(-3,5,0), \"Bright yellow\")\n\t\tlocal rWing = part(\"Wing_R\", Vector3.new(4,0.3,2), CFrame.new(3,5,0),  \"Bright yellow\")\n\t\tlWing.Transparency = 0.4\n\t\trWing.Transparency = 0.4\n\t\tweld(torso, lWing); weld(torso, rWing)\n\n\t\tlocal bv = Instance.new(\"BodyVelocity\")\n\t\tbv.Name     = \"AerialDrive\"\n\t\tbv.Velocity = Vector3.new(0,0,0)\n\t\tbv.MaxForce = Vector3.new(1e4,1e4,1e4)\n\t\tbv.Parent   = root\n\n\t\tlocal bg = Instance.new(\"BodyGyro\")\n\t\tbg.Name      = \"AerialGyro\"\n\t\tbg.MaxTorque = Vector3.new(1e5,1e5,1e5)\n\t\tbg.D         = 100\n\t\tbg.Parent    = root\n\tend\n\n\tlocal hum = Instance.new(\"Humanoid\")\n\thum.RigType   = Enum.HumanoidRigType.R6\n\thum.MaxHealth = cfg.maxHealth\n\thum.Health    = cfg.maxHealth\n\thum.WalkSpeed = cfg.walkSpeed\n\thum.Parent    = model\n\n\tmodel.PrimaryPart = root\n\treturn model\nend\n\nlocal function buildFromMarketplace(assetId, cfg)\n\tlocal ok, result = pcall(function()\n\t\treturn InsertService:LoadAsset(assetId)\n\tend)\n\tif not ok then\n\t\twarn(string.format(\"[PRIMER] Marketplace load failed (assetId=%d): %s\", assetId, tostring(result)))\n\t\treturn nil\n\tend\n\tlocal container = result\n\tlocal rig = container:FindFirstChildOfClass(\"Model\")\n\tif rig then rig.Parent = nil end\n\tcontainer:Destroy()\n\tif not rig then return nil end\n\n\tlocal hum = rig:FindFirstChildOfClass(\"Humanoid\") or Instance.new(\"Humanoid\", rig)\n\thum.MaxHealth = cfg.maxHealth\n\thum.Health    = cfg.maxHealth\n\thum.WalkSpeed = cfg.walkSpeed\n\thum.RigType   = Enum.HumanoidRigType.R6\n\n\tlocal hrp = rig:FindFirstChild(\"HumanoidRootPart\")\n\tif hrp then rig.PrimaryPart = hrp end\n\n\tif cfg.useBodyVelocity and hrp then\n\t\tif not hrp:FindFirstChild(\"AerialDrive\") then\n\t\t\tlocal bv = Instance.new(\"BodyVelocity\")\n\t\t\tbv.Name = \"AerialDrive\"; bv.Velocity = Vector3.new(0,0,0)\n\t\t\tbv.MaxForce = Vector3.new(1e4,1e4,1e4); bv.Parent = hrp\n\t\tend\n\t\tif not hrp:FindFirstChild(\"AerialGyro\") then\n\t\t\tlocal bg = Instance.new(\"BodyGyro\")\n\t\t\tbg.Name = \"AerialGyro\"; bg.MaxTorque = Vector3.new(1e5,1e5,1e5)\n\t\t\tbg.D = 100; bg.Parent = hrp\n\t\tend\n\tend\n\n\trig.Name = cfg.name\n\treturn rig\nend\n\n-- ── Main ──────────────────────────────────────────────────────────────────────\n\ntask.wait(1)\n\nlocal cfg = ARCHETYPES[ARCHETYPE]\nassert(cfg, \"[PRIMER] Unknown ARCHETYPE: \" .. tostring(ARCHETYPE))\n\nprint(string.format(\"[PRIMER] Building %s chassis (assetId=%d)…\", cfg.name, cfg.marketplaceAssetId))\n\nlocal rig\nif cfg.marketplaceAssetId and cfg.marketplaceAssetId > 0 then\n\trig = buildFromMarketplace(cfg.marketplaceAssetId, cfg)\nend\nrig = rig or buildBlockyR6Fallback(cfg)\n\nlocal templates = ensureFolder(ServerStorage, \"MonsterTemplates\")\nlocal existing  = templates:FindFirstChild(cfg.name)\nif existing then existing:Destroy() end\nrig.Parent = templates\n\nrig:SetAttribute(\"OMC_Archetype\",          ARCHETYPE)\nrig:SetAttribute(\"OMC_MaxHealth\",          cfg.maxHealth)\nrig:SetAttribute(\"OMC_UseBodyVelocity\",    cfg.useBodyVelocity or false)\nif cfg.useBodyVelocity then\n\trig:SetAttribute(\"OMC_HoverHeight\", cfg.hoverHeight or 18)\n\trig:SetAttribute(\"OMC_DiveSpeed\",   cfg.diveSpeed   or 60)\nend\nrig:SetAttribute(\"OMC_MarketplaceAssetId\", cfg.marketplaceAssetId or 0)\nrig:SetAttribute(\"OMC_PrimerVersion\",      \"1.0.0\")\n\nprint(string.format(\"[PRIMER] ✓ %s ready in ServerStorage.MonsterTemplates\", cfg.name))\n"},"errorMessage":"scriptType is required","errorCode":"EXECUTION_FAILED","executionTimeMs":0}
